
Al resolver se busca dejar una $y(x)$ en función de su **variable independiente** y **constantes**.

```graph TB

A-->B;

```

````mermaid
graph TD     A[Start] --> B{Is it?};     B -- Yes --> C[OK];     B -- No --> D[KO];     C --> E[End];     D --> E; ```   `
```