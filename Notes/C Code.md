## Basic

Start with:
```
#include <stdio.h>

int main(){

	return 0;
}
```

Commands:
```
printf("Pr")
```

## Variables and Format specifiers

```
int = whole number (%d)
float = decimal number (%f)
double = long float ($lf)
char = 'single character' (%c)
char[] = "string" (%s)
bool = true/false
```

## Pointers and memory